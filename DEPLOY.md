# رفع النسخة الجديدة

1. استبدل `index.html` و`app.js` و`reader.html` في مستودع GitHub.
2. اترك `admin.html` كما هو إذا كانت لوحة الإدارة الحالية تعمل لديك.
3. لا تحذف مجلد `assets` الموجود في مشروعك. احتفظ بشعارات Vortex وملف الغلاف الحالي داخله.
4. ارفع التغييرات إلى نفس فرع Cloudflare Pages الإنتاجي.
5. انتظر Deployment جديد في Cloudflare Pages.
6. اختبر:
   - الصفحة الرئيسية.
   - فتح فصل.
   - الإعدادات.
   - الرجوع للصفحة ثم فتح الموقع مرة أخرى للتحقق من «متابعة القراءة».

ملاحظة: أغلفة بطاقات الفصول تعتمد على أول صفحة محفوظة في جدول `pages`، لذلك لا تحتاج لتعديل قاعدة البيانات.
